<div>
  <h3>Add a booking</h3>
  <%= simple_form_for [@photographer, @booking] do |f| %>
    <%= f.input :date %>
    <%= f.button :submit, class: "btn btn-large btn-primary", "data-disable-with" => "Submitting.." %>
  <% end %>
</div>

