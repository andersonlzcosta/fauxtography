<div class="card-trip">
  <img src="<%= photographer.pictures[1] %>" />
    <div class="card-trip-infos">
      <div>
        <h2><%= photographer.name %></h2>
        <p><%= photographer.location %></p>
      </div>
  <h2 class="card-trip-pricing"><%= photographer.price_rate %></h2>
  <img src="<%= photographer.avatar %>" class="card-trip-user avatar" />
    </div>
</div>
